using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Interview5
{
    class Program
    {
        static void Main(string[] args)
        {
            /*string[] A = new string[] { "a", "a", "b", "b", "c" };
            string[] B = new string[] { "a", "c", "d" };

            Dictionary<string, int> frequencies = ArrayMethods.FindOccurancesInRepeatedArray(A, B);

            Console.WriteLine("Frequencies:");
            foreach (string key in frequencies.Keys)
            {
                Console.WriteLine(string.Format("{0}: {1}", key, frequencies[key]));
            }*/

            /*uint[] A = new uint[] { 0, 4, 7, 9, 11 };
            uint[] B = new uint[] { 1, 2, 3, 4 };

            uint[] result = ArrayMethods.MergeArrays(A, B);

            foreach (uint value in result)
            {
                Console.Write(string.Format("{0} ", value));
            }

            Console.WriteLine();*/

            Console.ReadLine();
        }
    }
}
